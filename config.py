import os

# Получаем токен из переменной окружения
token = os.getenv('token')

# Если токен не задан, выводим сообщение
if not token:
    print("Ошибка: токен не задан в переменной окружения!")
    print("Используйте: docker run -e token='ВАШ_ТОКЕН' или создайте .env файл")
