# Telegram Bot с SQLite БД

Учебный проект: Telegram бот с сохранением сообщений в базу данных. ЛР4-6 по Docker.

## 🚀 Быстрый запуск

```bash
# 1. Клонировать
git clone https://gitflic.ru/xisarad/tgbot.git
cd tgbot

# 2. Настроить токен
cp .env.example .env
nano .env  # вставьте токен от @BotFather

# 3. Запустить
docker-compose up -d

# 4. Логи
docker-compose logs -f
📁 Структура
text
telegram-bot/
├── app/           # Код бота
├── Dockerfile     # Сборка образа
├── docker-compose.yml # Запуск
├── .env          # Токен (не в git)
└── README.md     # Документация
🤖 Команды бота
/start - регистрация

/stats - статистика сообщений

/myid - ваш Telegram ID

/users - список пользователей (админ)

🏗️ Архитектура (ЛР6)
Сервер сборки (builder):

Полный доступ к коду

Собирает Docker образы

Сервер деплоя (deployer):

Только docker-compose.yml + .env

НЕТ исходного кода

🐳 Основные команды
bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f

# Сборка образа
docker build -t telegram-bot:latest .
🔧 Устранение проблем
Токен не работает:

Получите новый у @BotFather

Проверьте .env файл

Ошибка 409:

Уже запущен другой экземпляр бота

docker-compose down остановит всё

Docker не запускается:

bash
sudo systemctl start docker
sudo usermod -aG docker $USER
📚 Лабораторные
✅ ЛР4: Docker основы
✅ ЛР5: Секреты в .env
✅ ЛР6: Docker-compose + разделение серверов

Учебный проект. Для вопросов: issues на GitFlic.
