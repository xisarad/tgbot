# Учебный бот

После клонирования необходимо создать и активировать виртуальное окружение, например, в linux и mac:
python3 -m venv env
source env/bin/activate

## Установка:

pip install -r requirements.txt

Скопировать config.py.example в config.py и изменить его

## Запуск:

python main.py
## Работа с переменными окружения

После обновления бот использует переменные окружения для конфигурации.

### Быстрый запуск:
```bash
docker run -e token="ВАШ_ТОКЕН" telegram-bot


## Запуск с помощью Docker Compose

Для запуска бота используйте Docker Compose:

1. Создайте файл `.env` в корне проекта и укажите ваш токен:
   ```
   token="ВАШ_ТОКЕН"
   ```

2. Запустите контейнер:
   ```bash
   docker compose up -d
   ```

**Важно:**
- `Dockerfile` - для создания образа
- `compose.yml` - для запуска контейнера из образа
- Эти операции могут выполняться на разных машинах

В реальной работе образ собирается на сервере сборки и помещается в registry,
а затем запускается на отдельном сервере.

## 🗃️ Бот с базой данных (Дополнение ЛР6)

### Функциональность:
1. Регистрация пользователей при первом сообщении
2. Сохранение всех сообщений в SQLite БД
3. Статистика по пользователям
4. Команды:
   - `/start` - приветствие
   - `/stats` - ваша статистика
   - `/users` - список пользователей (админ)
   - `/myid` - показать ваш ID

### Структура БД:
- `users` - информация о пользователях
- `messages` - история сообщений

### Запуск:
```bash
# Создайте .env файл (если нет)
cp .env.example .env

# Запустите с docker-compose
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f
'EOF'

## Версии (Changelog)

### v1.1.1 (текущая)
- ✅ Исправлен счетчик сообщений в команде /stats
- ✅ Улучшена логика регистрации пользователей
- ✅ Сообщения теперь корректно считаются

### v1.1.0
- ✅ Добавлена поддержка SQLite базы данных
- ✅ Регистрация пользователей
- ✅ Команды /stats, /users, /myid
- ✅ Тома Docker для сохранения данных

### v1.0.0
- ✅ Базовый функционал бота
- ✅ Docker-образ и docker-compose
- ✅ Переменные окружения (.env)
