version: '3.8'
services:
  telegram-bot:
    build: .
    image: telegram-bot-compose
    container_name: telegram-bot-app
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      - TZ=Europe/Moscow
